Key Steps Implemented

Data loading and inspection

Exploratory Data Analysis (EDA)

Handling missing values & duplicates

Feature selection

Standardization (if needed)

Elbow Method to find optimal K

K-Means clustering

Cluster visualization





Dataset Columns

CustomerID

Gender

Age

Annual Income (k$)

Spending Score (1–100)



Outcome

The algorithm groups customers into distinct clusters such as:

High-income, high-spending customers

High-income, low-spending customers

Low-income, high-spending customers

Low-income, low-spending customers



#################
plt.plot(x, y, marker='o')  # line plot
plt.scatter(x, y)           # scatter plot
plt.bar(x, y)               # bar chart
plt.show()

plt.plot() → line plot (default)

plt.scatter() → scatter plot (good for clustering or points)

plt.bar() / plt.barh() → bar chart

plt.hist() → histogram

